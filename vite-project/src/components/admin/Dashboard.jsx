import React from "react";

const Dashboard = () => (
  <div style={{ padding: 20 }}>
    <h2>Admin Dashboard</h2>
    <p>Use the sidebar to manage users, generate quizzes, and view reports.</p>
  </div>
);

export default Dashboard;
